services:
  - type: web
    name: spacex-falcon9-dashboard
    env: python
    plan: free
    buildCommand: pip install -r dashboard/requirements.txt
    startCommand: gunicorn spacex-dash-app:server --chdir dashboard
