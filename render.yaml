services:
  - type: web
    name: spacex-falcon9-dashboard
    env: python
    plan: free
    buildCommand: pip install -r dashboard/requirements.txt
    startCommand: gunicorn spacex-dash-app:server --chdir dashboard --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
