# Core data science stack
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
matplotlib==3.8.4
seaborn==0.13.2

# Data collection
requests==2.32.3
beautifulsoup4==4.12.3
wget==3.2

# SQL EDA notebook
SQLAlchemy==2.0.52
ipython-sql==0.5.0
sqlparse==0.6.0
prettytable==3.18.0

# Geographic analysis
folium==0.20.0

# Dashboard
dash==4.4.1
plotly==6.9.0
joblib==1.4.2

# Modeling benchmark and interpretability
# 3.2.0 is pinned (not the newer 3.3.x/3.4.x also on PyPI) because those
# don't ship a manylinux wheel for the cp311/ubuntu-latest combination the
# CI runner uses -- pip's resolver silently excludes them there and fails
# with "no matching distribution" (confirmed directly from a failed CI run;
# this had gone unnoticed because it was only ever verified in a local
# macOS venv, which already had a newer xgboost pre-installed from outside
# PyPI).
xgboost==3.2.0
shap==0.46.0

# Notebook execution / CI
nbformat==5.9.2
nbclient==0.8.0
nbconvert==7.10.0
papermill==2.7.0
ipykernel==6.28.0

# Testing
pytest==7.4.4
